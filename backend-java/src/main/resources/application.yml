server:
  port: 3000
  error:
    include-message: always

spring:
  datasource:
    url: jdbc:mysql://${DB_HOST:114.132.65.101}:${DB_PORT:3306}/${DB_NAME:learning_game}?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:Hh415263}
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  mapper-locations: classpath:mapper/*.xml

admin:
  username: ${ADMIN_USERNAME:admin}
  password: ${ADMIN_PASSWORD:666666}

jwt:
  secret: ${JWT_SECRET:change-me-to-a-very-long-random-secret-at-least-32-bytes}
  expiration-minutes: ${JWT_EXPIRATION_MINUTES:10080}
